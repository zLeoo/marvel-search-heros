import React from 'react';
import Header from '../Header/Header';

const HeroSection = () => {
    return (
        <div>
            <Header/>
            <h2>Hero Section</h2>
        </div>
    );
};

export default HeroSection;